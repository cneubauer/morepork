namespace WaaS.Space.ViewModel;

/// <summary>
/// The hosting platform type for a webspace.
/// </summary>
public enum PlatformType
{
    /// <summary>Linux-based webspace platform.</summary>
    Linux = 1,
    //Windows = 2, // Commented out to avoid confusion, we don't support Windows for now
}

