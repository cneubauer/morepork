namespace WaaS.Space.Classic.ViewModel;

public enum DirectoryType
{
    /// <summary>User-owned directory within the webspace.</summary>
    User = 1,
    /// <summary>Managed directory controlled by the platform.</summary>
    Managed = 2,
}