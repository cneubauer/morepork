namespace WaaS.Webshield.ViewModel;

/// <summary>
/// The Webshield type on which the mapping should be provisioned.
/// These are usually different types of Webshield nodes that are available in the system.
/// </summary>
public enum WebshieldType
{
    Default,
    Cdn,
}
